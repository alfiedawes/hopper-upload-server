const tus=require("tus-node-server"),express=require("express"),server=new tus.Server;server.datastore=new tus.FileStore({path:"D:/Dropbox (The Box)/BOX Projects/XOB013 Hopper/3. Project files/Code/hopper-upload-server/files"});var app=express();const uploadApp=express();uploadApp.all("*",server.handle.bind(server)),app.use("/uploads",uploadApp),app.listen(port,host);